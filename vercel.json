{
  "buildCommand": "cd ai-resume-analyzer && npm install && npm run build",
  "outputDirectory": "ai-resume-analyzer/build/client",
  "installCommand": "cd ai-resume-analyzer && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
